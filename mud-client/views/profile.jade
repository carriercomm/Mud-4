extend layout

block content
  .grid-space-around
    a.add-char(href='/profile/' + user._id + '/newchar') Create new character

  if message && message.length
    .grid-space-around
      p.success= message
